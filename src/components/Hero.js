import React from 'react'
import styled from 'styled-components'

const Hero = () => {
    return (
        <HeroContainer>
            <HeroBackground>
                <VideoBackground src='' type='video/mp4' />
            </HeroBackground>
        </HeroContainer>

    )
}

export default Hero

const HeroContainer = styled.div`

`

const HeroBackground = styled.div`

`

const VideoBackground = styled.video`

`